# 2026-09 Project History

This file keeps chronological detail that should not live forever in `AI_HANDOFF.md`.

## 2026-09-21 — Repository reorganization

The repository was reorganized into a full-stack monorepo.

Main target structure:

```text
apps/mobile
apps/web
backend
scripts
```

The old root mobile source, nested web application, and uppercase `BACKEND` directory were moved into the normalized structure.

### Integrity audit

A repository-level content audit compared the pre-refactor tree against the reorganized tree.

Recorded result:

- old blobs: 1044
- new blobs at the audited point: 1048
- preserved same-content blobs: 1035
- SHA mismatches for expected preserved files: 0
- missing expected files: 0

Some old launcher files were intentionally removed/replaced during the refactor rather than treated as missing source.

### PR / merge history

- PR #1 — full-stack monorepo reorganization
  - merge commit: `60fd09b0c4e523663e66a7e78e6d3abdd64cfe5f`
- PR #2 — restore root `run.bat`
  - merge commit: `0794e9d57e0fccfa0e99b613257b213b48682099`
- PR #3 — keep Expo launcher visible on startup errors
  - merge commit: `f614bf2fa5cbe4123ce17f3612e05538053323a8`
- PR #4 — fix Windows Expo launcher quoting
  - merge commit: `e50d0d46f094325161258aba1c64e976fa113b86`
- PR #5 — verify/install local Expo CLI before launch
  - merge commit: `171c78ee5c52b458a4cb0378ae5404e8cb5e3a04`
- PR #6 — gitignored local MariaDB configuration workflow
  - merge commit: `1acf13ee81c66281a6ad70a974c2c383c4b9aa06`

Git remains the authoritative source for exact per-file diffs.

## 2026-09-21 — MariaDB runtime verification

The local database environment was verified as:

- MariaDB `10.4.32`
- database `kaitokid`
- 51 base tables
- application user `kaitokid@localhost`

A password mismatch initially caused MariaDB error 1045 and API.Customer EF/Pomelo failures.

The durable fix was not to commit a real password. Instead, a gitignored `backend/db.local.bat` workflow was added and loaded by the Windows launchers.

## 2026-09-21 — Expo/mobile startup fixes

Several separate launcher issues were identified and fixed:

1. Expo terminal closed too quickly to read failures.
2. Nested Windows CMD quoting produced `The syntax of the command is incorrect.`.
3. `node_modules` existed while `node_modules/.bin/expo.cmd` was missing, so the old script incorrectly skipped dependency installation.

The current launcher checks for the real Expo executable, installs dependencies if needed, and runs the local CLI.

## 2026-09-21 — Verified end state

After the DB and Expo launcher fixes:

- API.Auth started successfully.
- API.Customer connected to MariaDB.
- Expo/Metro started successfully.
- The Expo Web build rendered the mobile application.
- Categories and product data loaded from the backend on the mobile home screen.

This verified state was promoted into `docs/AI_HANDOFF.md`.


## 2026-09-21 — Expo Web media/console cleanup

Investigated browser console noise from the mobile Expo Web build.

- Existing slider files `slide_1.jpg`, `slide_2.jpg`, and `slide_3.jpg` were present in `apps/web/public` but were not exposed by API.Customer on port 5265.
- Product seed URLs such as `/products/jean-nu-1.jpg` referenced files that did not exist in either the current repository or the pre-refactor tree.
- API.Customer was updated to serve `apps/web/public` as shared media and to return a branded placeholder for missing seeded product images.
- The React Native Web `pointerEvents` deprecation message was confirmed as upstream navigation noise; a narrow development-only warning filter was added rather than forcing a breaking React Navigation major-version upgrade.


## 2026-09-22 — PHASE 1: Chốt branding + dữ liệu

Định vị thương hiệu được chốt thành **KaitoKid — thời trang trẻ em 0–12 tuổi**.

Thay đổi chính:

- dữ liệu seed MariaDB được chuyển từ shop nam/nữ người lớn sang catalog trẻ em;
- 19 sản phẩm mẫu giữ nguyên ID/SKU để bảo toàn quan hệ nhưng đổi tên, mô tả, slug, ảnh logic, size và nhóm tuổi;
- category, collection, banner, lookbook, menu, review/order snapshot mẫu được đồng bộ;
- thêm migration `20260922_phase1_kids_branding.sql` cho database local hiện tại, không DROP bảng;
- mobile có `constants/brand.ts` và app display name `KaitoKid`;
- thêm `docs/BRAND.md` và `docs/ROADMAP.md`.

PHASE tiếp theo: **PHASE 2 — Nâng cấp Home**.


## 2026-09-22 — PHASE 2: Nâng cấp Home mobile

Home mobile được tái cấu trúc theo flow shop thời trang trẻ em:

- header brand + account/cart;
- cart badge đọc dữ liệu thật khi có token;
- login được nối lại qua AuthContext để lưu session;
- hero auto-slide 4.5 giây + counter + pagination;
- promo/value strip;
- root categories;
- discovery tiles;
- ProductCard mới với sale/new/hot, stock, swatch màu, rating/sold;
- skeleton loading;
- social block và brand footer.

PHASE 2 không triển khai wishlist/Add-to-cart thật hoặc Cart screen để giữ đúng ranh giới roadmap. Những phần đó được để cho PHASE 4–5.

Môi trường công cụ không thể clone repo qua network để chạy TypeScript compiler; branch đã được static-review và cần runtime validation trên máy development sau khi pull.

PHASE tiếp theo: **PHASE 3 — Product Detail hoàn chỉnh**.


## 2026-09-22 — PHASE 3: Product Detail hoàn chỉnh

Product Detail mobile được nâng cấp từ màn hiển thị cơ bản thành flow chọn sản phẩm đầy đủ:

- gallery ảnh swipe/thumbnail/counter;
- màu sắc và size có selected/unavailable state;
- size guide trẻ em 90–150;
- quantity selector;
- tồn kho tổng và tồn kho theo biến thể;
- discount/saving/SKU/rating/sold;
- mô tả được làm sạch từ HTML;
- bảng thông tin/specs;
- review đã duyệt ở chế độ read-only;
- share sản phẩm;
- related products;
- skeleton và retry state.

Backend Product Detail được mở rộng để trả `VariantInventory` từ `TonKhoBienThe`, trả đầy đủ dữ liệu review cần cho UI và cho phép xem sản phẩm `out-of-stock`.

Không có migration database mới trong PHASE 3. Seed hiện chưa có ảnh phụ hoặc tồn kho biến thể mẫu nên UI dùng fallback cho đến khi dữ liệu đó được bổ sung.

PHASE tiếp theo: **PHASE 4 — Wishlist + Add to Cart**.


## 2026-09-22 — PHASE 4: Wishlist + Add to Cart

Mobile được nối với Wishlist/Cart API thật:

- thêm `ShoppingContext` toàn app;
- ProductCard và Product Detail có wishlist thật;
- thêm màn `/wishlist`;
- login hỗ trợ redirect về màn gọi xác thực;
- Product Detail Add to Cart dùng đúng size/màu/số lượng;
- feedback lỗi/thành công trực tiếp trên Product Detail;
- cart badge đồng bộ trên Home và bottom tab;
- Cart tab tạm thời hiển thị count thật trong khi chờ PHASE 5.

Backend được siết invariant tồn kho:

- map `SanPham.SoLuongDaGiu`;
- thêm `AvailableStock` vào Product DTO;
- reserve đồng thời ở product và variant;
- validate quantity, status, size, color và variant;
- update/remove/expiry/checkout đều giải phóng reserve;
- thêm migration idempotent `20260922_phase4_cart_reservation.sql`.

PHASE tiếp theo: **PHASE 5 — Cart thật**.


## 2026-09-22 — PHASE 5: Cart thật

Mobile Cart được thay từ màn count tạm thành màn quản lý giỏ hàng đầy đủ:

- tải item thật và derive cart badge từ `ShoppingContext.cartItems`;
- chọn item/select all, subtotal theo selection;
- tăng/giảm số lượng, xóa một/xóa nhiều;
- chuyển item đã chọn sang Wishlist;
- reservation countdown và refresh khi giữ hàng hết hạn;
- combo discount từ backend;
- cross-sell dùng ProductCard và bắt buộc mở Product Detail để chọn variant;
- CTA checkout lưu selected cart IDs để PHASE 6 xử lý partial checkout đúng contract.

Backend giữ nguyên `/api/cart/cross-sell` dạng `CartItemDTO` cho Web và thêm `/api/cart/cross-sell-products` trả `ProductDTO` đầy đủ cho Mobile, tránh phá contract hiện có và không phải tự dựng variant.

Không có migration database mới trong PHASE 5.

PHASE tiếp theo: **PHASE 6 — Checkout + Address + Shipping + Payment**.


## 2026-09-22 — PHASE 6: Checkout + Address + Shipping + Payment

Checkout Mobile được nối end-to-end từ selection của Cart:

- `CheckoutContext` giữ selected Cart IDs/items và state checkout scoped;
- Address CRUD + default address;
- shipping quote theo địa chỉ và backend re-quote khi tạo order;
- coupon + combo chỉ tính trên selected items;
- partial checkout chỉ xóa/trừ stock/release reserve item đã chọn; item còn lại giữ nguyên trong Cart;
- COD tạo order trực tiếp;
- ATM/bank transfer có instructions, countdown, status polling, cancel và dev-only simulate;
- QR chỉ hiển thị khi store setting cung cấp thật, không hard-code gateway trong Mobile;
- Order Success hiển thị mã đơn và tóm tắt;
- Web checkout được bổ sung structured shipping address để dùng contract server-side mới.

Backend payment status/instructions được giới hạn theo chính chủ order; payment method cũng được kiểm tra theo store setting.

Không có migration database mới trong PHASE 6.

Theo yêu cầu workflow, mọi thay đổi UI từ đây phải đọc `docs/UI_UX.md` và dùng `skill/.codex/skills/ui-ux-pro-max/SKILL.md` trước khi code. Quy tắc đã được ghi vào `AGENTS.md`, `docs/UI_UX.md` và `docs/DECISIONS.md`.

PHASE 6 cũng thêm regression tests cho selected partial checkout, selected ID không hợp lệ và fallback all-cart để bảo vệ Web hiện tại. Môi trường connector chưa chạy được `dotnet test`/Expo runtime; cần xác nhận runtime sau khi pull.

PHASE tiếp theo: **PHASE 7 — Orders + Tracking**.
